<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory="storage/framework/cache/phpunit">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="WHATSAPP_DRIVER" value="log"/>

        <!--
            Penyimpanan & log DIALIHKAN, bukan cuma database.

            Sebelum ini hanya DB yang diisolasi (sqlite :memory:). Penyimpanan
            dan log tetap menunjuk produksi, sehingga test yang lupa memanggil
            Storage::fake() menulis berkas sungguhan ke storage/app/private —
            213 berkas sampah menumpuk di sana sebelum ketahuan — dan setiap
            galat test tercetak ke laravel-*.log yang sama dengan produksi,
            membuat 110 dari 113 baris ERROR hari itu bukan galat sungguhan.

            Yang menumpuk cuma sampah; tapi jalur tulisnya sama dengan jalur ke
            berkas nyata, dan itu yang tidak boleh dibiarkan.
        -->
        <env name="FILESYSTEM_DISK" value="testing"/>
        <env name="LOG_CHANNEL" value="testing"/>
        <env name="LOG_STACK" value="testing"/>
    </php>
</phpunit>
